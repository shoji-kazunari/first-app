# パチンコシミュレーター

公開スペックをもとに、初当たり・連チャン・出玉をボタン操作で疑似体験できるサイト。
GitHub Pages（静的ホスティング）で `https://shoji-kazunari.github.io/first-app/` に公開している。

## 作りの前提

- **ビルド工程なし。** 素のHTML/CSS/JSをそのまま配信する
- 各JSはIIFEで、`window.PachiSim` 名前空間にぶら下げる。読み込み順はHTMLの`<script>`順が正
  （例外は `core/firebase.js` のみ。ESモジュールで読み、得た機能を `PachiSim.fb` へ橋渡しする）
- **機種はデータ。** `core/stateEngine.js` が `machine.states` / `onHit` / `onExhausted` を
  解釈して動く。機種ファイルはロジックを持たない純粋なデータ
- サーバー側の処理は書けない。外部と通信するものはブラウザから直接送る形にしかできない

| 置き場所 | 中身 |
|---|---|
| `assets/js/core/` | エンジン・乱数・設定・永続化・検証 |
| `assets/js/ui/` | 保留・リール・データランプなどの描画部品 |
| `assets/js/pages/` | TOPと機種ページのコントローラ |
| `assets/js/data/machines/` | 機種データ（1機種1ファイル） |
| `machines/<slug>/index.html` | **生成物**。直接編集しない |
| `tools/` | 機種ページの生成スクリプトと雛形 |
| `tests/` | ブラウザで開くだけで走る自作テスト |

---

# 新台の追加ルール

## 1. スペックは必ず一次情報から取る（最重要）

**標準ソースは 1geki.jp。** 記憶・検索結果の要約・推測で数値を埋めてはいけない。

理由は、**間違いに後から気づけないから**。`machineValidator.js` が検査するのは形式だけで、
「振り分けが実機と違う」「出玉が違う」は素通りする。数値が間違っていても動いてしまい、
それらしく間違ったシミュレーターになる。ここだけは自動では守れない。

- 必要な数値が1つでも確認できなければ、**埋めずに依頼者に聞く**
- 円グラフなど画像でしか書かれていない値は、画像として読み取って確認する

### 情報源に到達できないとき

実行環境のネットワークポリシー次第で、1geki.jpがegressプロキシに遮断されることがある
（`CONNECT tunnel failed, response 403`）。**迂回してはいけない。** 次のどちらかで受け取る。

- **スクリーンショット**（画像は読み取れる。これが一番早い）
- テキストの貼り付け

## 2. 集める数値

- 正式名称 / メーカー / 導入年 / カテゴリ（スマパチ・ST機など）
- 読み仮名、別名（検索に使う。略称や通称も入れる）
- 大当たり確率（通常時 / RUSH・ST中）
- ST・時短の回転数
- **ラウンド振り分け** … ヘソ（通常時）と電チュー（RUSH中）を分けて、
  各パターンの「ラウンド数・出玉・振り分け率・当選後の移行先」
- STや時短を消化しきったあとの移行先（通常に戻るのか、別の時短に入るのか）
- 1000円あたりの回転数の目安

RUSHが複数種類ある、突入率が100%でないといった機種は、状態を分けて表現する必要がある。
その場合はゲームフロー図も確認する。

## 3. 出典をデータファイルに書き残す

ファイル冒頭のコメントに、**どこから取ったか・どう確認したか・迷った箇所**を書く。
手本は `assets/js/data/machines/eva17-hajimari.js`。

数値の正しさを機械で検査できない以上、次に見た人が追試できることが唯一の担保になる。

## 4. 手順

```
1. assets/js/data/machines/<slug>.js を1つ書く（既存ファイルをコピーするのが早い）
2. node tools/build-machine-pages.js       # 機種ページとTOPの読み込み行を生成
3. tests/index.html をブラウザで開く        # 全機種を横断して検査する。全部passすること
4. 実際に3速度（普通・速い・当たりまで）で回し、コンソールエラーが出ないことを見る
```

機種ページとTOPの一覧行は自動で作られる。スクリプト側に機種名を書き足す必要はない。

データに不備があれば、ページを開いた時点で日本語の例外になる
（検査項目は `tools/README.md` を参照）。

## 5. 転落式の機種を足すとき

状態に `onFall`（転落抽選）を持たせると、その状態は自動的に次の見た目になる。
機種データ側に演出の設定を書き足す必要はない。

- **リーチ演出を出さない。** 転落式のRUSHは超短縮変動で、リーチになった時点で
  当たりか転落かが決まる。「リーチ中のドキドキ」という時間が存在しないため。
  大当たりのときだけ、リーチの間を挟まずに3つ揃う
- **保留の色変化予告を出さない。** 次が当たりか転落かしかない状態で煽っても示せるものが無い

同じ機種でも通常時は普通のゲームなので、リーチも色保留もそのまま出る
（`onFall` を持つ状態だけが対象）。

## 6. やってはいけないこと

- **`machines/<slug>/index.html` を直接編集しない。** これは生成物で、次の再生成で
  黙って上書きされる。見た目を変えるときは `tools/machine-page.template.html` を直す。
  食い違いは `node tools/build-machine-pages.js --check` が検出する
  （実際に一度、速度変更ラベルが生成物にだけ入っていて消える寸前だった）
- **出典の確認できない数値を入れない**（1に戻る）

---

## その他の決めごと

- **アクセス解析・公開先URL（canonical）・独自ドメインへの移行** … `tools/README.md`
- **測定IDと公開先URL** は `assets/js/core/config.js` の1箇所にだけ書く
- 外部へ送る項目を増やしたら、必ず `privacy/index.html` も直す
- コミットメッセージ・PR・コード内に、使ったモデル名を書かない
